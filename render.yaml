services:
  - type: web
    name: ultirepo-backend
    runtime: docker
    rootDir: ultirepo-ai/backend
    dockerfilePath: Dockerfile
    dockerContext: .
    plan: free
    region: singapore
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: LLM_PROVIDER
        value: gemini
      - key: EMBEDDING_PROVIDER
        value: local

  - type: web
    name: ultirepo-frontend
    runtime: static
    rootDir: ultirepo-ai/frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    plan: free
    region: singapore
    envVars:
      - key: VITE_API_BASE_URL
        value: https://ultirepo-backend.onrender.com
      - key: VITE_API_WS_HOST
        value: ultirepo-backend.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
